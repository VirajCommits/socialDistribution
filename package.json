{
  "dependencies": {
    "marked": "^14.1.3",
    "turndown": "^7.2.0"
  },
  "scripts": {
    "heroku-postbuild": "npm install && npm run build",
    "build": "vue-cli-service build"
  },
  "engines": {
    "node": "18.x",   
    "npm": "8.x"    
  }
}
